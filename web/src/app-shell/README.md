# App shell and PWA — Workstream I

Owned by I. The service worker, offline care-page cache and install prompt land
in S9; the shell itself (`+layout.svelte`, `Nav.svelte`) already lives in
`web/src/lib/ui/` and `web/src/routes/`.

Offline scope, decided in S0: care instructions and the current Morning Rounds
are cached; maps, charts and photos are not.
