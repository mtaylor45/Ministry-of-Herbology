# Map component — Workstream H

Owned by H. The Leaflet wrapper lands in S7: `CRS.Simple` for floor plans, a
georeferenced image overlay for the property survey, zone polygons and specimen
pins on both.

`api/grounds/` already serves mock layers and pins against the frozen contract,
so this component can be built and tested before upload and calibration exist.
