<script lang="ts">
  /** The component gallery lives in `$ui/gallery` — Workstream I's own
   *  directory. This route is the three lines needed to reach it; if A would
   *  rather the gallery hung somewhere else, only this file moves. */
  import Gallery from '$ui/gallery/Gallery.svelte';
</script>

<Gallery />
