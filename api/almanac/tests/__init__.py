"""Almanac API tests.

A package marker, because with no root pytest config a test module is named by
its basename and two files with one name anywhere in the repository stop the
whole run at collection.
"""
